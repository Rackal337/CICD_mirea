name: Run Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m venv venv
          source venv/bin/activate
          pip install -r requirements.txt

      - name: Run tests
        run: |
          source venv/bin/activate
          pytest

        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const core = require('@actions/core');
            const { Octokit } = require('@octokit/core');
            const { create } = require('@octokit/plugin-graphql');
            const { OctokitPluginTelegram } = require('octokit-plugin-telegram');

            const octokit = new Octokit({
              auth: ${{ secrets.GITHUB_TOKEN }},
              plugins: [
                OctokitPluginTelegram,
              ],
            });

            octokit.telegram.sendMessage({
              chat_id: '7147287402:AAE1zPQqOQgdr3xuhcHSDZcdqV4-cm1Yth4',
              text: 'Тесты пройдены успешно! 💪',
            });
            core.info("Сообщение отправлено в Telegram");
